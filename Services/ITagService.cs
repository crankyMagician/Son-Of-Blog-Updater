using SonOfBlogUpdater.Models;

namespace SonOfBlogUpdater.Services
{
    /*
     * The TagService is an interface that you will need to implement. 
     * This interface should contain a method called GetTagsForBlog
     * that takes in a blogId and returns a list of tags for the given blog.
     */
    public interface ITagService
    {
       // Task<List<TagModel>> GetTagsForBlog();
        // Task<TagModel> GetTagById(int id);
       // Task<int> CreateTag(TagModel tag);
       // Task UpdateTag(TagModel tag);
        //Task DeleteTag(int id);

    }

}
